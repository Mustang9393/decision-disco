{
  "name": "decision-disco",
  "version": "1.0.0",
  "description": "Decision Disco – static site with serverless API",
  "scripts": {
    "start": "node server.js",
    "build": "mkdir -p dist && cp index.html script.js style.css dist/ || true"
  },
  "dependencies": {}
}